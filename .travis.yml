language: go
go:
  - 1.x

sudo: required
services:
  - docker

before_install:
  - chmod +x ./run_tests.sh

env:
  - GOVERSION=1.11

install: true

cache:
  directories:
    - ~/.cache

script:
  - sudo apt-get update && apt-get upgrade -y && apt-get install -y rsync
  - ls
  - ./run_tests.sh docker
